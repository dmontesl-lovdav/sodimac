# Database Credentials (to be replaced by actual secrets in GCP)
DATASOURCE_USERNAME=$DATASOURCE_USERNAME
DATASOURCE_PASSWORD=$DATASOURCE_PASSWORD

# JWT Configuration (to be replaced by actual secrets in GCP)
JWT_PUBLIC_KEY_B64=$JWT_PUBLIC_KEY_B64

# Additional Secrets
KEYCLOAK_REALM_CERT=$KEYCLOAK_REALM_CERT