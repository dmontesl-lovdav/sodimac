apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${PROJECT_NAME}
spec:
  template:
    spec:
      serviceAccountName: ${WORLOAD_IDENTITY_UAT}
      containers:
        - name: ${PROJECT_NAME}

