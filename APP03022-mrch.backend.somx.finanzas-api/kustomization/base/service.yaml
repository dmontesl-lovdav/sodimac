apiVersion: v1
kind: Service
metadata:
  name: ${PROJECT_NAME}
spec:
  type: ClusterIP
  selector:
    app: ${PROJECT_NAME}
  ports:
    - protocol: TCP
      name: http
      port: 8082
      targetPort: 8082

