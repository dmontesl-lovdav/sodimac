apiVersion: v1
kind: Service
metadata:
  name: ${PROJECT_NAME}
  annotations:
    cloud.google.com/neg: '{"exposed_ports": {"8080":{"name": "mrch-backend-somx-utils-api-neg"}}}'
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8082
  selector:
    app: ${PROJECT_NAME}
