# Perfil DOCKER — batch corriendo DENTRO de la red Docker (red sodimac-net).
# Resuelve hostnames de contenedor (sin NAT host->contenedor) => SAPITO Oracle conecta.
# Puertos INTERNOS de contenedor (mssql 1433, oracle 1521), no los mapeados al host.
spring:
  datasource:
    jdbc-url: jdbc:h2:mem:invoicedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false

server:
  port: 8085

datasource:
  sodimac-sap:
    jdbc-url: jdbc:sqlserver://sodimac-mssql:1433;databaseName=SODIMAC_SAP_DEV;encrypt=false;trustServerCertificate=true
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: SA
    password: 'Sodimac2026#Dev'
  sapito:
    jdbc-url: jdbc:oracle:thin:@sodimac-oracle:1521/FREEPDB1
    driver-class-name: oracle.jdbc.OracleDriver
    username: UODSRMX
    password: uodsrmx47q8
  i213:
    jdbc-url: jdbc:sqlserver://sodimac-mssql:1433;databaseName=AdmIF213ProdDB;encrypt=false;trustServerCertificate=true
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    username: SA
    password: 'Sodimac2026#Dev'

batch:
  scheduler:
    enabled: true
    cron: "0 30 7 * * *"
    max-retries: 0
    retry-interval-minutes: 1
    max-execution-minutes: 15
  datasource:
    url: jdbc:sqlserver://sodimac-mssql:1433;databaseName=SODIMAC_BATCH_DEV;encrypt=false;trustServerCertificate=true
    username: SA
    password: 'Sodimac2026#Dev'

fbc:
  portal:
    url: http://localhost:8081
    mock-enabled: true

alert:
  notification:
    email:
      enabled: false

logging:
  level:
    root: INFO
    com.invoicesync: DEBUG
