# ===============================
# CONFIGURACIÓN PARA DESARROLLO
# ===============================

spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

# SECURITY CONFIGURATION
security.enabled=${FINANZAS_JWT_ENABLED:false}

pac.detecno.name=DETECNO
pac.carbajal.name=CARBAJAL

# DATABASE CONFIGURATION
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5434}/${DB_NAME:b2b_portal}?currentSchema=tenant_fiscal&ssl=${DB_SSL:false}
spring.datasource.username=${DB_USER:wwwb2bportal}
spring.datasource.password=${DB_PASS:b8@qU0YM1HU>}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.default_schema=tenant_fiscal

# LOGGING LEVEL (más detallado en dev)
logging.level.root=INFO
logging.level.Console=INFO
logging.level.com.sodimac=DEBUG

# ACTUATOR (completo en dev)
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# UTILS API CONFIGURATION (para dev local)
utils.api.url=${UTIL_API_URL:http://localhost:3712/api}
